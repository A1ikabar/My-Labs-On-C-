#pragma once

#include <iostream>
#include <cctype>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <string>

using namespace std;

//void displayBySearch(string name);
void enter(), init_list(), display();
void /*update(),*/ input(int i);
int menu();
const int SIZE = 100;
